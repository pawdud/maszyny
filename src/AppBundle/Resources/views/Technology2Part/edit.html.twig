{{ form_start(form, { 'action': app.request.requestUri, 'attr': {'novalidate': 'novalidate' } } ) }}  
    <div class="form-group">
        <label>Technologia:</label>
        <div>
            {{ technology2part.technology.name }}
        </div>
    </div>
    {{ form_row(form.isCompleted) }}              
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Zapisz <i class="fa fa-floppy-o"></i></button>
    </div>
{{ form_end(form) }}    